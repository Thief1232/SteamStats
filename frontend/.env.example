# Пусто/не задано = фронт работает на локальных фикстурах (src/data/*.json).
# Как только бэкенд поднят — раскомментируйте и укажите его адрес,
# ничего в коде менять не нужно (см. /API_CONTRACT.md).
# VITE_API_BASE_URL=http://localhost:8000
